import { IncreaseImageResolutionContainer } from "../container/create-handle-image/increase-image-resolution";

const IncreaseImageResolution = () => {
    return <IncreaseImageResolutionContainer />;
};

export default IncreaseImageResolution;
