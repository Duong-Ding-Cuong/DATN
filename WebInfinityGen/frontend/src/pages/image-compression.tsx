import { ImageCompressionContainer } from "../container/create-handle-image/image-compression";

const ImageCompression = () => {
    return <ImageCompressionContainer />;
};
export default ImageCompression;
